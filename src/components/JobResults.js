import React from 'react';


const JobResults = () => {

  return (

    <section>
      <h3 className="title has-text-centered animated shake slow">Job Results</h3>
      {/* <div className="columns is-multiline">
        {locations.map((loc, i) => <div className="result" key={i}>

          <img className="image" src={loc.company_logo}/>
          <h4 className="has-text-left purple">Location: {loc.title}</h4>
          <h5 className="has-text-left purple">Company: {loc.company}</h5>
          <h5 className="has-text-left purple">Location: {loc.location}</h5>
          <a href={loc.url} target="_new"><button>apply now</button></a>


        </div>)}
      </div> */}
    </section>


  );
};

export default JobResults;
